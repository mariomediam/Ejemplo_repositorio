let nombres=["Mario", "Rosy", "Camila", "Andrea", "Victoria", "Lourdes", "Alexandra"]

/*
//---------------------------------------------------
nombres.forEach(function(name, indice, arreglo){
    console.log(`${name} - ${indice} - ${arreglo}`)
})
*/

let numeros=[100,200,500,90,800];

/*
//---------------------------------------------------
let cuenta = numeros.map((monto, indice, arreglo) => {
    return monto * 1.05;
})

console.log(cuenta);
*/

/*
//---------------------------------------------------
let position = nombres.indexOf("Camila");

console.log(position);
*/

/*
//---------------------------------------------------
nombres.splice(3, 2)

console.log(nombres);

console.log(nombres.splice(-1, 2));
*/